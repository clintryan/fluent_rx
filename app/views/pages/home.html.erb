<%= render 'pages/hero' %>

<%= render 'pages/benefits' %>

<%= render 'pages/courses' %>

<%= render 'pages/testimonials' %>

<!-- Final CTA Section -->
<section class="bg-gray-50 py-24">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl text-center">
      <h2 class="text-lg font-semibold leading-8 tracking-tight text-blue-accent">Ready to Start?</h2>
      <p class="mt-2 text-5xl font-bold leading-tight tracking-tight text-text-dark mb-6">
        Invest in your professional voice.
      </p>
      <p class="text-xl leading-8 text-gray-600 mb-10">
        Clear, confident communication is the most powerful tool for your career. Let's build yours together.
      </p>
      <%= render 'shared/button', text: "Book a free consultation", url: "javascript:void(0)", type: :primary, options: { onclick: "Calendly.initPopupWidget({url: 'https://calendly.com/fluentrx/infosession'});return false;", class: "text-lg px-8 py-4" } %>
    </div>
  </div>
</section>